# Codex Assistant Web 0.10.0 设计 QA

- 验收日期：2026-07-22（Asia/Shanghai）
- 生产地址：`https://codex-assistant-windows.dzass2.chatgpt.site`
- 桌面视口：`1600 × 900`
- 移动视口：`390 × 844`
- 产品状态：主题专用版；环境检测、官方入口不变、按需手动应用、本机导入、安全恢复

## 验收结果

- 首页只展示换肤能力，没有代理、模型调度或监控入口。
- 页面明确说明本机环境检测、官方入口保持不变、完整重开后需再次点击“应用主题”，以及不驻留、不自动拉起 Codex 的边界。
- 首屏标题、主题前后对照、下载按钮和安全说明在桌面端完整可见。
- `一键换肤`、`本机导入`、`安全恢复` 三个演示标签可切换，选中状态与内容同步。
- 移动端标题与预览卡片没有横向溢出；页面 `scrollWidth` 等于 `clientWidth`。
- 安装包链接指向 `Codex-Assistant-0.10.0-x64-setup.exe`；构建后由同步脚本写入版本、大小和 SHA-256，并与实际文件一致。
- 页面语义结构、键盘焦点、降低动态效果与高对比偏好均保留。
- 社交分享图为 `1200 × 630`，文字和界面元素没有被裁切。
- 生产页加载完成后没有发现阻塞性交互或布局问题。

## 视觉安全边界

- 主题背景不覆盖正文、图标或交互控件。
- 卡片与输入区使用半透明材质和原生语义色，保持文字对比度与点击命中。
- 页面不承诺修改 Codex 功能；主题仅改变视觉层。

## 最终结论

待构建 0.10.0 安装包并运行同步脚本后复验下载工件的大小和 SHA-256；其余页面文案与视觉边界已更新。
